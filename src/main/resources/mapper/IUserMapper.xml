<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="cn.aynu.java2.weibo.Dao.IUserMapper">

    <select id="selectUserByLogin" resultType="User">
        select * from user where email=#{email} and password=#{password}
    </select>
    
    <insert id="insertUser">
        insert into user (avatar,email,nick_name,sex,birthday,password) values (#{avatar},#{email},#{nickName},#{sex},#{birthday},#{password})
    </insert>
</mapper>